# modelPicker

<!-- BEGIN:mini-deploy -->
## Mini Deployment

This repo deploys to the home Mac mini via `git push mini`. Master doc: `~/Projects/TOOLS/MINI_DEPLOY.md`.

- **Slug / branch**: `modelpicker` / `main`
- **Mini paths**: bare `~/srv/modelpicker.git`, working tree `~/srv/modelpicker`
- **Stack**: Node (only `dev` script, no `start`)
- **Auto-start (pm2)**: ❌ — add a `start` script and `ecosystem.config.js` to enable.

GitHub origin is `Nanman5/modelpicker.git`.
<!-- END:mini-deploy -->
